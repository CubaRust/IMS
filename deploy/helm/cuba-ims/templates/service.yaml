apiVersion: v1
kind: Service
metadata:
  name: {{ include "cuba-ims.fullname" . }}
  labels:
    {{- include "cuba-ims.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cuba-ims.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
